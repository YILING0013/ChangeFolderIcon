<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="ChangeFolderIcon.UserControls.IconControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ChangeFolderIcon.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:ChangeFolderIcon.Models"
    AllowDrop="True"
    Drop="Root_Drop"
    DragOver="Root_DragOver"
    Width="120" Height="140"
    mc:Ignorable="d">

    <Grid CornerRadius="12"
          BorderThickness="1" Padding="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="32"/>
        </Grid.RowDefinitions>

        <Image Source="{x:Bind Icon.IconSource, Mode=OneWay}"
               Stretch="Uniform" Margin="4"/>

        <TextBlock Grid.Row="1" Text="{x:Bind Icon.Name, Mode=OneWay}"
                   HorizontalAlignment="Center" TextTrimming="CharacterEllipsis"/>
    </Grid>
</UserControl>
