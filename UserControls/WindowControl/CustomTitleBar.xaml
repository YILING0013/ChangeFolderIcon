<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="ChangeFolderIcon.UserControls.WindowControl.CustomTitleBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ChangeFolderIcon.UserControls.WindowControl"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:animatedvisuals="using:Microsoft.UI.Xaml.Controls.AnimatedVisuals"
    mc:Ignorable="d">

    <Grid x:Name="AppTitleBar">
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="LeftPaddingColumn" Width="0"/>
            <ColumnDefinition x:Name="HeaderColumn" Width="Auto"/>
            <ColumnDefinition x:Name="IconColumn" Width="Auto"/>
            <ColumnDefinition x:Name="TitleColumn" Width="Auto"/>
            <ColumnDefinition x:Name="LeftDragColumn" Width="9*"/>
            <ColumnDefinition x:Name="ContentColumn" Width="20*"/>
            <ColumnDefinition x:Name="RightDragColumn" Width="8*"/>
            <ColumnDefinition x:Name="FooterColumn" Width="Auto"/>
            <ColumnDefinition x:Name="RightPaddingColumn" Width="0"/>
            <!-- 系统按钮预留（右） -->
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="2">
            <Image x:Name="TitleBarIcon" Source="ms-appx:///Assets/icon/app_icon.ico" Width="26" Height="26" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="4,0"/>
        </Grid>
        <Grid Grid.Column="3">
            <TextBlock x:Name="TitleBarTextBlock" x:Uid="TitleBarTitleText" Text="应用标题" Style="{StaticResource CaptionTextBlockStyle}" VerticalAlignment="Center"/>
        </Grid>
        <Grid Grid.Column="4" x:Name="LeftDragArea"/>
        <Grid Grid.Column="5" x:Name="TitleBarSearchArea">
            <Grid.Resources>
                <ResourceDictionary>
                    <ResourceDictionary.MergedDictionaries>
                        <ResourceDictionary Source="ms-appx:///Microsoft.UI.Xaml/DensityStyles/Compact.xaml" />
                    </ResourceDictionary.MergedDictionaries>
                </ResourceDictionary>
            </Grid.Resources>
            <AutoSuggestBox x:Name="TitleBarSearchBox" x:Uid="itleBarSearchBox" QueryIcon="Find" PlaceholderText="搜索" VerticalAlignment="Center"  Margin="0,4,0,0"/>
        </Grid>
        <Grid Grid.Column="6" x:Name="RightDragArea"/>
        <Grid Grid.Column="7">
            <!-- 设置按钮 - 使用AnimatedIcon -->
            <Button x:Name="SettingsButton" 
                x:Uid="SettingsButton"
                VerticalAlignment="Stretch" 
                HorizontalAlignment="Right"
                Background="Transparent"
                BorderThickness="0"
                CornerRadius="0"
                Margin="0,0,10,0"
                Click="SettingsButton_Click"
                ToolTipService.ToolTip="设置">
                <AnimatedIcon x:Name="SettingsAnimatedIcon" 
                          Width="18" 
                          Height="18"
                          Foreground="{ThemeResource TextFillColorPrimaryBrush}">
                    <AnimatedIcon.Source>
                        <animatedvisuals:AnimatedSettingsVisualSource/>
                    </AnimatedIcon.Source>
                    <AnimatedIcon.FallbackIconSource>
                        <SymbolIconSource Symbol="Setting"/>
                    </AnimatedIcon.FallbackIconSource>
                </AnimatedIcon>
            </Button>
        </Grid>
    </Grid>
</UserControl>
