<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="ChangeFolderIcon.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ChangeFolderIcon"
    xmlns:controls="using:ChangeFolderIcon.UserControls.WindowControl"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="ChangeFolderIcon">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <controls:CustomTitleBar x:Name="CustomTitleBarControl" Grid.Row="0"  VerticalAlignment="Top" Height="32"/>
        <NavigationView x:Name="NavView"
                        Grid.Row="1"
                        Margin="0,1,0,0"
                        PaneDisplayMode="Left"
                        IsBackButtonVisible="Collapsed"
                        IsSettingsVisible="False">

            <NavigationView.PaneHeader>
                <Button x:Name="SelectFolderButton"
                        Content="选择文件夹"
                        Click="SelectFolderButton_Click"
                        HorizontalAlignment="Stretch"
                        Margin="8,0,4,0"/>
            </NavigationView.PaneHeader>

            <NavigationView.MenuItems>
                <NavigationViewItem x:Name="NavigationViewTip" Content="点击上方按钮选择一个文件夹" IsEnabled="False"/>
            </NavigationView.MenuItems>

            <!-- 右侧内容区域 -->
            <Frame x:Name="ContentFrame"/>

            <NavigationView.FooterMenuItems>
                <NavigationViewItem Content="About" Icon="ViewAll" />
                <NavigationViewItem Content="Help" Icon="Help" />
            </NavigationView.FooterMenuItems>
        </NavigationView>
    </Grid>
</Window>
